# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
jobs:
  build:
    working_directory: ~/repo
    docker:
      - image: circleci/golang:1.16
    steps:
      - checkout
      - restore_cache:
          keys:
            - go-mod-v4-{{ checksum "go.sum" }}
      - run:
          name: Install Dependencies
          command: go mod download
      - save_cache:
          key: go-mod-v4-{{ checksum "go.sum" }}
          paths:
            - "/go/pkg/mod"
      - run:
          name: Setup test files
          command: make
      - run:
          name: Run tests
          command: |
            # Make test and artifact directories
            mkdir -p /tmp/test-reports
            mkdir -p /tmp/test-artifacts
            # Run tests
            gotestsum --junitfile /tmp/test-reports/unit-tests.xml -- \
              -coverprofile=/tmp/test-artifacts/coverage.txt ./...
            # Generate coverage report
            go tool cover \
              -html=/tmp/test-artifacts/coverage.txt \
              -o=/tmp/test-artifacts/coverage.html
      - store_test_results:
          path: /tmp/test-reports
      - store_artifacts:
          path: /tmp/test-artifacts
      - run:
          # ref: https://github.com/codecov/example-go/blob/master/.circleci/config.yml
          name: Upload coverage to Codecov
          command: |
            bash <(curl -s https://codecov.io/bash) \
              -f /tmp/test-artifacts/coverage.txt
